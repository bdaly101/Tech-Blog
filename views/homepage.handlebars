<section class="">
    <h2>Homepage</h2>
    {{#if logged_in}}
    <p>Logged in as: {{user.username}}</p>
    {{else}}
    <a href="/login">Login/SignUp</a>
    {{/if}}
  <section class="blog-list">
    
    {{#each blogs as |blog|}}
    {{> blog-details}}
    {{/each}}
  </section>
</section>